# Progress Ledger

The single authoritative status ledger. States: Proposed / Approved / In Progress / Blocked / Implemented / Verified / Superseded. No status is duplicated anywhere else — other docs link here.

| Unit ID | Description | State | Entry Dependencies | Exit-Criteria Evidence | Last Updated |
|---|---|---|---|---|---|
| Phase 1 | Repo scaffold & local infra skeleton | Verified | none | `scripts/smoke_test.sh` — ALL SMOKE CHECKS PASSED on a genuine cold start (`docker compose down -v`) and a subsequent warm run, re-verified 2026-08-21 after fixing an OTel-span-flush regression (a forceful process kill was destroying the span before its periodic batch-export could flush it) and a Windows PID-resolution bug | 2026-08-21 |
| Phase 2 | Identity, org data model & seeded directory | Verified | Phase 1 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml down -v` then `scripts/smoke_test.sh`) after the smoke test's stale `health_check` table check (dropped by migration 0002) was fixed to check for `employee` instead — smoke test now reports ALL SMOKE CHECKS PASSED on cold start, proving the 0001→0002→0003 migration chain applies cleanly to an empty database and the original Phase 1 checks (infra health, FastAPI /health, OTel startup span, Next.js build) still work. Against that same fresh container: `apps/api/tests` 19 passed, `packages/authz/tests` 9 passed, `packages/telemetry/tests` 2 passed (30 total); `scripts/seed_employees.sh --seed 42 --employees 75` seeded 75 employees. Manual curl spot-check against a running `uvicorn` instance, using employee IDs freshly assigned by this seed run (not reused from any prior container) — admin (employee id 96) received all 75 employees from `GET /directory/employees`, a no-grant employee (id 111) received exactly 1 record (themselves), and a request with no `X-Debug-Employee-Id` header was rejected with 401. Additionally confirmed on real GitHub Actions infrastructure (not just locally): pushed to `origin/master`, CI run [32593266056](https://github.com/shreshta-p/ResolveGrid/actions/runs/32593266056) passed all 3 jobs (`web`, `api` — including the Postgres service container, migration chain, and full pytest suite — and the new `authz` job) on its first-ever execution against Phase 2 code | 2026-08-22 |
| Phase 3 | Ticketing core: state machine, schema, hash-chained audit log, ticket API, employee-submit + analyst-triage UI | Verified | Phase 2 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml down -v` then `scripts/smoke_test.sh`) — ALL SMOKE CHECKS PASSED, proving the full 0001→0005 migration chain (including the new ticketing tables and FK indexes) applies cleanly to an empty database. Against that same fresh container: full suite 61 passed (`apps/api/tests` 31, `packages/authz/tests` 14, `packages/contracts/tests` 14, `packages/telemetry/tests` 2); `pnpm --filter @resolvegrid/web lint/typecheck/build` all clean. `scripts/seed_employees.sh --seed 42 --employees 75` initially failed with a `ForeignKeyViolation` deleting an employee already referenced by a real `AuditLog` row (from the test suite's own committed fixture activity) — this surfaced a real bug in `generate_org()`'s blanket wipe, not just a known/accepted limitation as previously assumed; fixed by excluding audit-referenced employees (and the department/location/team rows they'd otherwise orphan) from the delete, then re-verified the seed script succeeds and is idempotent even with that residue present, run twice with no reset in between. Full manual browser walkthrough (Playwright MCP) against this same fresh, freshly-seeded container: employee 104 submitted a real ticket (#44) via `/tickets/new`; department-38-scoped analyst 105 saw it in their queue at `/tickets`, opened `/tickets/44`, and transitioned it open→in_progress→closed through the real UI (the in_progress state correctly offered all three legal next states — `resolved`/`open`/`closed` — confirming the earlier `NEXT_STATUS_OPTIONS` fix is live); employee 640 (no role grant, different department) received a 403 attempting to even view `/tickets/44`, not just transition it — the state machine and its authorization boundary cannot be bypassed from the client. Audit chain confirmed real, not just present: `resolvegrid_api.audit.verify_chain_integrity()` returned `True` against the live database immediately after this walkthrough's create + 2 transitions. Confirmed on real GitHub Actions: pushed to `origin/master`, CI run [32692159115](https://github.com/shreshta-p/ResolveGrid/actions/runs/32692159115) passed all 3 jobs (`web`, `api`, `authz`) against the full Phase 3 commit history. | 2026-08-24 |
| Phase 4 | Model gateway: self-hosted LiteLLM proxy → local Ollama `qwen3:14b`, `ModelCall`/`PricingVersion` schema, ticket-summarization endpoint with real cost/latency capture | Verified | Phase 3 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml down -v`, including the `ollama_data` volume, then `scripts/smoke_test.sh`) — ALL SMOKE CHECKS PASSED, proving the full 0001→0007 migration chain (including the new `pricing_version`/`model_call` tables and the pricing-alias fix) applies cleanly to an empty database, and all 5 infra containers (including the new `litellm` service with its now-fixed Python-based healthcheck) came up healthy with no manual intervention. Against that same fresh container: full suite 72 passed (`apps/api/tests` 42, `packages/authz/tests` 14, `packages/contracts/tests` 14, `packages/telemetry/tests` 2); `pnpm --filter @resolvegrid/web lint/typecheck` clean; `scripts/seed_employees.sh --seed 42 --employees 75` succeeded; `qwen3:14b` (9.3GB) re-pulled into the fresh Ollama container. Full manual browser walkthrough (Playwright MCP) against this fresh, freshly-seeded, freshly-pulled-model container: employee 104 submitted a real ticket (#16) via `/tickets/new`; on its detail page, clicking Summarize showed the "Summarizing..." disabled-button loading state, and the first (cold) attempt genuinely timed out at 60s (`Error 502: LLM gateway error: timed out`) — this is the exact, already-documented cold-start limitation, not a new bug, and it recorded a real `status="error"` `ModelCall` row rather than crashing; a second click (model now warm) succeeded in 1269ms, returning a real summary ("A printer on the 3rd floor is offline...") with badge `1269ms · 64→33 tokens · $0.0000`, matching `model_call` row id 7 exactly (`pricing_version_id=1`, proving the Task 4 pricing-alias fix holds under real, non-mocked traffic, not just mocked tests). A no-grant employee (id 124, unrelated to the ticket) received a 403 attempting to even load `/tickets/16`, confirming the authorization boundary holds. Confirmed on real GitHub Actions: pushed to `origin/master`, CI run [32815377453](https://github.com/shreshta-p/ResolveGrid/actions/runs/32815377453) passed all 4 jobs (`web`, `api`, `authz`, `contracts`) against the full Phase 4 commit history. Scope note: Langfuse/Prometheus/Grafana stand-up deliberately deferred to a later phase (see `docs/DECISION_LOG.md`). | 2026-08-25 |
| Phase 5 | Cloud provider fallback routing: Anthropic (`cloud-primary`) + OpenAI (`cloud-fallback`) behind LiteLLM, `ModelCall.fallback_occurred`/`serving_model_group` capture | Verified | Phase 4 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml down -v` then `scripts/smoke_test.sh`, now fixed to pass `--env-file .env` so real cloud provider keys actually reach the `litellm` container instead of silently resolving empty — a real gap in the script itself, found and fixed during this verification) — ALL SMOKE CHECKS PASSED, proving the full 0001→0008 migration chain applies cleanly to an empty database. Against that same fresh container: full suite 76 passed (`apps/api/tests` 46, `packages/authz/tests` 14, `packages/contracts/tests` 14, `packages/telemetry/tests` 2); `pnpm --filter @resolvegrid/web lint/typecheck` clean; `scripts/seed_employees.sh --seed 42 --employees 75` succeeded. **Real, live, non-mocked forced-fallback verification** (not just mocked unit tests): called `llm_gateway.complete()` directly against the real running LiteLLM proxy with real Anthropic/OpenAI keys, first with `cloud-primary` temporarily pointed at an invalid model (reverted immediately after) — this exposed and led to fixing a genuine, previously-undiscovered bug (`"think": false` was being sent to every provider including Anthropic/OpenAI, which reject unrecognized parameters outright with a 400, meaning cloud routing would have been completely non-functional in production despite passing every mocked test). After the fix: the forced-failure call correctly returned `provider="openai", fallback_occurred=True, serving_model_group="cloud-fallback"`; the real (working) `cloud-primary` route separately confirmed `provider="anthropic", fallback_occurred=False, serving_model_group="cloud-primary"`. Full suite re-ran clean after the fix (48 `apps/api/tests`, no regressions). Confirmed on real GitHub Actions: pushed to `origin/master`, CI run watched and passed all 4 jobs (`web`, `api`, `authz`, `contracts`) against the full Phase 5 commit history. Scope notes (deliberate, documented, not oversights — see `docs/DECISION_LOG.md`): no `routing_reason`/`fallback_reason` free-text field (LiteLLM never exposes the actual upstream error text, only that/which a fallback occurred); per-provider scoped virtual keys/budget enforcement deferred; `summarize_ticket`'s hardcoded `"ollama"` in its error path and span attribute left as-is since no caller can request a non-default model yet. | 2026-08-25 |
| Phase 6 | Minimal LangGraph agent workflow: `classify_intent` → `compose_response` → `finalize` graph, `AsyncPostgresSaver` checkpointing, `AgentRun`/`Span` schema, `/chat` endpoint + web page, checkpoint-restore test, 18-case golden classification set | Verified | Phase 5 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml --env-file .env down -v` then `scripts/smoke_test.sh`) — ALL SMOKE CHECKS PASSED, proving the full 0001→0009 migration chain (including the new `agent_run`/`span` tables) applies cleanly to an empty database. This run also caught and fixed a real, previously-undiscovered bug: `scripts/smoke_test.sh`'s own FastAPI-startup check failed with `psycopg.InterfaceError` because a real `uvicorn` process on Windows hard-codes `ProactorEventLoop` regardless of any `--loop` flag (verified directly against uvicorn's source — there is no CLI-flag fix), bypassing the `WindowsSelectorEventLoopPolicy` `main.py` already sets; fixed with a new `scripts/run_api_dev.py` launcher that monkey-patches uvicorn's loop factories on Windows only. Against that same fresh container: full suite 98 passed (`apps/api/tests` 55, `packages/authz/tests` 14, `packages/contracts/tests` 14, `packages/telemetry/tests` 2, `services/agent-orchestration/tests` 13 — the last of these newly wired into CI as its own job, matching the `contracts` job's precedent); `pnpm --filter @resolvegrid/web lint/typecheck` clean; `scripts/seed_employees.sh --seed 42 --employees 75` succeeded; `qwen3:14b` (9.3GB) re-pulled into the fresh Ollama container. Full manual browser walkthrough (Playwright MCP) against this fresh, freshly-seeded, freshly-pulled-model container: employee 110 asked "What is a firewall?" via `/chat`, the "Thinking..." disabled-button loading state showed correctly through the ~75s cold-load delay (2 sequential LLM calls internally), and a real answer rendered with the required "no ticket or company-specific knowledge base yet" caption. Verified directly in the database (not just the UI): a real `AgentRun` row (`status="completed"`, correct `input_text`/`output_text`/`principal_employee_id=110`) and 3 real `Span` rows (`classify_intent`/`compose_response`/`finalize`, all `status="success"`), plus confirmation the real `AsyncPostgresSaver` checkpointer tables (`checkpoints`, `checkpoint_writes`, `checkpoint_blobs`, `checkpoint_migrations`) exist and were created cleanly on this fresh database. The checkpoint-restore test itself (`apps/api/tests/test_checkpoint_restore.py`) is the actual proof of durable resumability — state written by one graph/checkpointer instance is correctly read back by a completely independent second instance, per `docs/adr/0002-postgres-checkpointer.md`'s methodology — and passed as part of the 55 `apps/api/tests`. Confirmed on real GitHub Actions: pushed to `origin/master`, CI run [33046053934](https://github.com/shreshta-p/ResolveGrid/actions/runs/33046053934) passed all 5 jobs (`web`, `api`, `contracts`, `agent-orchestration`, `authz`) against the full Phase 6 commit history. Scope notes (deliberate, documented, not oversights — see `docs/DECISION_LOG.md`): stages 4-10 (retrieval/tools/approvals) not yet implemented, matching the approved plan's own "no knowledge base yet" framing; `risk_level` has no real enforcement yet (deliberate sequencing); a pre-existing `get_principal` employee-validation gap (already accepted since Task 3's review) was independently reproduced twice during manual `/chat` verification but not fixed here, since it's a cross-cutting `deps.py` concern affecting every router, not specific to this phase. | 2026-08-27 |
| Phase 7 | Knowledge ingestion + baseline hybrid retrieval: `Document`/`DocumentVersion`/`Chunk`/`Embedding`/`IngestionRun` schema (migrations 0010/0011), structure-aware Markdown chunker (`services/retrieval`), `nomic-embed-text` embedder, pgvector cosine search + Postgres lexical search + RRF fusion (k=60), authz-aware retrieval filtering baked into SQL, deterministic sufficiency check, Arq-based ingestion pipeline + 8-doc/36-chunk seed corpus, retrieval wired into the agent graph (`classify_intent → retrieve → compose_response → finalize`) with citation-mapped `/chat` answers, retrieval evaluation harness + recorded baseline. `docs/RAG_INGESTION.md` (new, includes the public-source attribution log) documents the as-built pipeline. | Verified | Phase 6 | Re-verified from a genuinely fresh state (`docker compose -f infra/docker-compose.yml --env-file .env down -v`, including `postgres_data`/`ollama_data` volumes, then `scripts/smoke_test.sh`) — ALL SMOKE CHECKS PASSED, proving the full 0001→0011 migration chain (including the new knowledge schema and the `chunk.search_vector` generated column) applies cleanly to an empty database and all 5 infra containers came up healthy with no manual intervention. `qwen3:14b` (9.3GB) and `nomic-embed-text` (274MB) were both re-pulled into the fresh Ollama container (confirmed via `ollama list`). Against that same fresh container: full suite 188 passed across every affected package (`apps/api/tests` 107, `packages/telemetry/tests` 2, `packages/authz/tests` 17, `packages/contracts/tests` 14, `services/agent-orchestration/tests` 20, `services/retrieval/tests` 28 — the last newly wired into CI as its own job, matching `agent-orchestration`'s Phase 6 precedent); `scripts/seed_employees.sh --seed 42 --employees 75` succeeded. Seed corpus ingested fresh via `python -m resolvegrid_api.ingestion_worker` (the committing, non-rollback direct-call path): `IngestionRun` reported `status=completed documents_processed=8 chunks_created=36`; verified independently by direct SQL count against the live DB (`document`=8, `document_version`=8, `chunk`=36, `embedding`=36) — exactly matching Task 9's recorded baseline corpus size. The retrieval evaluation harness (`resolvegrid_api.eval_retrieval.run_eval`, run via a one-off script against this same persisted session rather than `eval_retrieval.main()`, which always re-ingests-and-rolls-back) reproduced Task 9's recorded baseline **exactly**: recall@5=1.0000, precision@5=0.2000, MRR=0.9524, nDCG@5=0.9643 over the same 14 answerable / 4 no-good-match golden-case split, zero authz leakage (`must_not_appear` never violated), zero distractor-outranks-correct-answer cases — expected and unsurprising, since the corpus/chunker/embedding model are all deterministic given the same pinned versions, not a coincidence requiring investigation. Full manual browser walkthrough (Playwright MCP) against this fresh, freshly-seeded, freshly-ingested, freshly-pulled-model container (`apps/api` via `scripts/run_api_dev.py`, `apps/web` dev server — Next.js fell back to port 3001 since 3000 was already bound by an unrelated local process): employee 130 (David Brown, Security department) asked "What is the current Kestrel VPN client software policy?" via `/chat` and received a real, cited answer (inline `[chunk:222]` citing the *current* "Kestrel VPN Access Policy (v2)" document, with 5 total citations listed spanning both the v1-deprecated and v2 chunks, caption "Answer grounded in 5 knowledge-base citations..."). Re-confirmed the authz boundary in the same fresh session (cheap, per this task's own guidance): employee 128 (Matthew Arroyo, Platform Engineering, no role grant) asked the identical question and received zero citations to either Security-scoped VPN policy document — only their own department's on-call-escalation doc and the public "What Is a VPN" reference doc, with the model correctly stating it had no information to answer the VPN-specific question, confirming the SQL-level authz filter holds under a real, non-mocked `/chat` call. **Deliberate final-state decision**: unlike Tasks 6/7/9's "restore to empty" convention, the ingested corpus (8 docs/36 chunks) and the 75 seeded employees were left in the dev database after this walkthrough rather than rolled back — this is Phase 7's last task, later phases (tool-calling/approvals per the master plan's stage 8+) are likely to build against a populated knowledge base and directory rather than an empty one, and re-ingesting/re-seeding from scratch is already a one-command operation (`ingestion_worker.py`'s direct-call path, `seed_employees.sh`) if a future phase needs a clean slate instead. Dev-server processes (uvicorn on 8000, Next.js on 3001) were stopped after the walkthrough; the docker infra containers (with the ingested/seeded data) were left running.

**Addendum — a real bug found and fixed while closing out this task**: this task's implementer agent was interrupted by a session limit partway through writing `README.md`, having already completed the fresh-state verification, docs, and the numbers above. The continuing session found Docker Desktop itself had stopped in the interim (restarted it; the named-volume data survived intact, confirmed by direct row counts matching what's reported above) and, before committing, re-ran the full `apps/api` suite as an extra sanity check — which is *not* what the established fresh-state procedure calls for once seeding/ingestion has already run, and doing so surfaced a real, previously-latent bug: `apps/api/tests/test_ingestion_worker.py`'s Arq-job test and `test_chat_api.py`'s `seed_corpus_ingested` fixture both cleaned up after themselves by **matching the seed corpus's document titles**, not by tracking which rows they themselves created. `ingest_document`'s idempotency means re-running ingestion against an already-populated corpus creates zero new rows (a title match just resolves to the existing `Document`), so title-based cleanup silently deleted the deliberately-persisted corpus (and, via a cascading `test_seed.py` interaction over `manager_id` FK chains — the same narrow, already-documented seed.py gap from Phase 6's closing — reset the employee directory too) the moment the suite was re-run against a populated DB. Fixed in both files by tracking created rows via an id-watermark (`id > max-id-before-this-test-ran`), matching the pattern already used for `IngestionRun` in the same tests — verified the fix is correct by wiping to empty, confirming `apps/api/tests` still passes clean at **107/107**, then re-seeding/re-ingesting as the final step and confirming nothing was deleted this time.

This also surfaced a second, structural (not fixable by the cleanup logic) constraint worth recording as an explicit rule for future fresh-state verifications, not just this run's incident: `apps/api/tests/test_retrieval.py`'s fixed-content lexical/vector-search tests assert *exact* result lists against hand-constructed fixture chunks, and genuinely fail if real seed-corpus content is present in the DB when they run (confirmed directly — re-running the full suite with the corpus already ingested produces real assertion-mismatch failures in that file, not flakiness). **The full test suite must only ever be run against an empty-of-Phase-7-content database; seeding employees and ingesting the corpus must be strictly the last, untested steps of any verification sequence** (exactly the order this task's own numbers above already followed — smoke test → full suite 188 passed → *then* seed → *then* ingest → *then* eval/walkthrough — the incident was caused only by re-running tests a second time afterward, which the original verification never did). Final confirmed dev-DB state after restoring proper ordering: `document`=8, `chunk`=36, `embedding`=36, `employee`=78, `agent_run`=0 (the walkthrough's own `AgentRun`/`Span` rows were incidental test-session artifacts, not meaningful state, and were cleaned up — this does not conflict with the "leave the corpus/directory populated" decision above, which was about the knowledge base and employee directory, not ephemeral chat-session rows). The retrieval evaluation harness was re-run against the freshly re-ingested corpus one more time after the fix and reproduced the exact same baseline (recall@5=1.0000, precision@5=0.2000, MRR=0.9524, nDCG@5=0.9643, zero leakage) confirmed above.

Confirmed on real GitHub Actions: pushed to `origin/master`, CI run [33185045183](https://github.com/shreshta-p/ResolveGrid/actions/runs/33185045183) passed all 6 jobs (`web`, `api`, `contracts`, `agent-orchestration`, `authz`, `retrieval`) against the full Phase 7 commit history. | 2026-08-28 |
