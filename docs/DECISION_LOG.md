# Decision Log

| Date | Decision | Rationale | Ref |
|---|---|---|---|
| 2026-08-20 | Modular monolith over microservices; pnpm+uv workspaces over Nx/Turborepo; centralized `packages/authz` over per-service RLS or an external policy engine | See ADR | `docs/adr/0001-modular-monolith-topology.md` |
| 2026-08-20 | ADK scoped to exactly one bounded in-process specialist agent, no A2A in v1 | ADK's own docs show it wants to own orchestration itself (conflicts with LangGraph); A2A is not a sub-agent/tool-call protocol and this platform has no cross-trust-boundary service yet | Approved architecture plan §4 |
| 2026-08-20 | Anthropic primary / OpenAI fallback behind LiteLLM | User has API access/budget for both | Approved architecture plan §4 |
| 2026-08-21 | Deferred adding a naming_convention to Base.metadata | Retrofitting one after migrations already exist risks autogenerate proposing to rename already-applied constraints; needs its own dedicated task, not a quick fix | apps/api/src/resolvegrid_api/models/base.py |
| 2026-08-21 | Directory API resolves the requesting principal via a temporary `X-Debug-Employee-Id` header, not real authentication | No auth system exists yet (JWT/session auth is a later phase per the agent-workflow's stage 1); Phase 2's exit criteria require role-restricted endpoints to exist and be tested now. The header is explicitly documented as insecure scaffolding in `apps/api/src/resolvegrid_api/deps.py`'s docstring and must be replaced before any non-local deployment. | `apps/api/src/resolvegrid_api/deps.py` |
| 2026-08-23 | `apps/api` allows a hardcoded dev-only CORS origin list (`localhost:3000`/`3001`), unconditionally applied with no environment gating | The API had no CORS middleware at all, which blocked every browser-based request from `apps/web`'s Next.js dev server (preflight failure), including the required manual browser verification for Phase 3 Task 5. No settings/config abstraction exists yet in `apps/api` (only ad-hoc `os.environ.get` calls), so this follows the same inline-comment-as-documentation pattern as the `X-Debug-Employee-Id` shim above rather than introducing a new config system. `allow_credentials=False`, so the wildcard `allow_methods`/`allow_headers` carries no credential-leak risk. Must become a real, environment-driven origin allowlist before any non-local deployment. | `apps/api/src/resolvegrid_api/main.py` |
| 2026-08-23 | Ticket state machine centralized in `packages/contracts`, not duplicated per-consumer | `ALLOWED_TRANSITIONS` is the single source of truth `apps/api` enforces against and `apps/web` reads for its transition-button affordance. A hand-copied frontend table already drifted once during Task 6 (missing `in_progress`→`closed`) before being caught in review — concrete proof duplication was the wrong call, not just a theoretical risk. | `packages/contracts/src/resolvegrid_contracts/tickets.py` |
| 2026-08-23 | In-memory (not Redis-backed) rate limiting accepted for ticket creation | `apps/api` runs as a single process today (modular-monolith ADR, no horizontal scaling yet), so a per-process sliding window is sufficient without adding a Redis dependency before anything needs it. Known related gap: rate-limit keys are never evicted and the limit is trivially bypassed by an attacker sending a fresh `X-Debug-Employee-Id` per request (that header is unauthenticated). Not worth solving independently of the auth shim it depends on. Revisit trigger: `apps/api` running as more than one process, or real authentication landing (fix both together). | `apps/api/src/resolvegrid_api/rate_limit.py` |
| 2026-08-23 | Hash-chained audit log's lack of row locking under concurrent writers accepted for Phase 3's scope | `record_audit_event` reads the previous row and writes the new one in the caller's transaction with no explicit lock; concurrent writers could both read the same "last" row and compute conflicting chains. Acceptable for Phase 3's single-writer-at-a-time scope. Revisit trigger: real concurrent ticket-mutation load, via `SELECT ... FOR UPDATE` or a serializable isolation level. | `apps/api/src/resolvegrid_api/audit.py` |
| 2026-08-24 | `generate_org()`'s seed wipe now protects audit-referenced employees (and their department/location/team) instead of blanket-deleting the whole table | Previously documented as an accepted local-only limitation (repeated dev runs after real ticket activity would hit a `ForeignKeyViolation` deleting an employee still referenced by `audit_log.actor_id`, since the log is append-only by design). This stopped being a narrow edge case once Phase 3 Task 7's own mandated fresh-state verification sequence — run the full test suite, *then* seed — started hitting it on every single run, not just repeated ones, since `test_tickets_api.py`'s fixture always commits real audit-referenced employees during the test run that precedes seeding. Fixed by excluding audit-referenced employees (and the department/location/team rows they'd otherwise orphan) from the wipe, rather than reworking the append-only audit design. Narrow, not a general transitive-closure solver — see the comment in `seed.py` for what it deliberately does not handle (e.g. manager_id chains, since no protected employee here ever sets one). | `apps/api/src/resolvegrid_api/seed.py` |
| 2026-08-24 | Ticket-summary prompts include raw, unsanitized ticket subject/message text with no prompt-injection defense | The approved architecture plan's prompt-injection boundary (treating retrieved/tool content as untrusted) is explicitly scoped to Phase 8's RAG pipeline and its adversarial test suite, not this phase's first non-agentic call. Blast radius today is low: no tool-execution loop exists yet for injected text to hijack, and the analyst viewing the summary already has raw read access to the same ticket body — the residual risk is a misleading/manipulated summary shown to a human who can already see the source, not a system compromise. Revisit before any later phase wires ticket content into an agent/tool-calling loop, where the same unsanitized pattern would be a real boundary violation, not just cosmetic. | `apps/api/src/resolvegrid_api/routers/tickets.py` (`summarize_ticket`) |
| 2026-08-24 | `ModelCall` rows have no actor field; `summarize_ticket` doesn't call `record_audit_event` | Every mutation elsewhere in this router (`create_ticket`, `transition_ticket`) is audited; this read-only endpoint currently is not, breaking that convention. Accepted for now because it's read-only (no state change to attribute) and Ollama calls are free, so cost accountability doesn't yet matter. Revisit once Phase 5 adds paid providers behind the same gateway — at that point, knowing who triggered a billed call becomes a real accountability requirement, not just consistency for its own sake. | `apps/api/src/resolvegrid_api/routers/tickets.py` (`summarize_ticket`), `apps/api/src/resolvegrid_api/models/telemetry.py` |
| 2026-08-24 | Deferred standing up Langfuse (trace/eval/cost UI) and Prometheus/Grafana to a later phase, descoping Phase 4's originally-planned exit criteria | The approved plan's Phase 4 exit criteria called for a call "visible... in ResolveGrid's ModelCall table, Langfuse, and Grafana." Standing up Langfuse's own stack (ClickHouse + MinIO + web/worker services) and Prometheus+Grafana is a materially large, separable infra addition, and per the approved architecture's own design, Langfuse is explicitly "a secondary deep-debugging surface, never the sole source of truth" — the ResolveGrid-owned `ModelCall`/`PricingVersion` tables are. Phase 4's actual deliverable (a real, non-mocked cost/latency badge) only requires the owned schema. Revisit trigger: any later phase that specifically needs Langfuse's trace-debugging UI or Prometheus/Grafana's infra-metrics dashboards, not just more `ModelCall` volume. | `docs/superpowers/plans/2026-08-24-phase4-model-gateway.md` ("Scope decision") |
| 2026-08-24 | Ollama model choice: `qwen3:14b` (9.3GB, fits the 4080's 12GB VRAM at 100% GPU residency) | Matched the approved architecture plan's stated choice exactly; no fallback to a smaller model was needed. Its default "thinking" mode was found to cost ~60x more tokens/latency for a short non-reasoning task and is disabled per-call via `"think": false` — see `apps/api/src/resolvegrid_api/llm_gateway.py`'s docstring for the measured numbers. | `infra/litellm/config.yaml` |
| 2026-08-25 | `ModelCall.fallback_occurred`/`serving_model_group` (bool + string), not a `routing_reason`/`fallback_reason` free-text field, per the approved plan's Phase 5 wording | Empirically verified against real, live Anthropic/OpenAI calls: LiteLLM signals a fallback via response HEADERS only (`x-litellm-attempted-fallbacks` count, `x-litellm-model-group`), never the body, and the upstream failure's actual reason text is never available anywhere in a successful fallback response — LiteLLM swallows it internally after a successful retry. A text-reason column would have nothing genuine to store; the schema only captures what's actually observable (that/which a fallback occurred). Revisit trigger: a future need to see the specific upstream error would require a separate lookup against LiteLLM's own spend/log API by call-id, not a bigger response payload. | `apps/api/src/resolvegrid_api/models/telemetry.py`, `docs/superpowers/plans/2026-08-25-phase5-cloud-fallback.md` |
| 2026-08-25 | Deferred per-provider scoped virtual keys + budget enforcement (the approved plan's Phase 5 "Security" bullet) to a later phase | Both `cloud-primary` and `cloud-fallback` currently share the single `LITELLM_MASTER_KEY` for all proxy access — there is no per-provider spend cap or key isolation yet. Implementing this properly means generating and storing real per-provider virtual keys via LiteLLM's `/key/generate` API (backed by its own dedicated Postgres DB, already set up in Phase 5 Task 1) and wiring budget limits — a genuinely separate, non-trivial addition, not a quick config tweak, and out of proportion to this phase's actual exit criterion (proving fallback routing works). Real money is now at stake with real providers wired up (unlike Ollama), which makes this a live gap, not a hypothetical one — flagged here rather than silently skipped, matching the Langfuse deferral's precedent. This also sharpens the still-open `summarize_ticket` actor-tracking gap noted 2026-08-24: with real paid calls now flowing, knowing who triggered a billed request is a real accountability requirement, not just a consistency nice-to-have. Revisit trigger: before any non-local/shared deployment, or before spend volume makes a shared unmetered key a real risk. | `infra/litellm/config.yaml`, `infra/docker-compose.yml` |
| 2026-08-25 | `summarize_ticket`'s `gen_ai.system` span attribute and error-path `ModelCall.provider` remain hardcoded to `"ollama"`, not derived from `llm_gateway`'s new `_MODEL_GROUP_TO_PROVIDER` map | Not a live bug today: `summarize_ticket` has no way to request anything but `llm_gateway.DEFAULT_MODEL` (no endpoint/UI lets a caller choose `cloud-primary`), so `"ollama"` is currently accurate in both spots. Left alone rather than exposing new public API surface in `llm_gateway.py` to serve a caller (a cloud-model-aware ticket-summarize path) that doesn't exist yet — that would be premature coupling to an undecided future feature, not a real fix. Revisit trigger: whenever `summarize_ticket` (or any future caller) gains the ability to request a non-default model — at that point both the span attribute and the error-path `ModelCall` construction must derive from the requested model instead of a literal. | `apps/api/src/resolvegrid_api/routers/tickets.py` (`summarize_ticket`) |
| 2026-08-26 | Agent graph nodes call an injected `complete_fn: Callable[[str], str]`, never `llm_gateway` directly; `classify_intent` uses manual JSON-prompt + Pydantic validation, not a LangChain chat-model wrapper | Full reasoning in `docs/adr/0002-postgres-checkpointer.md` (Decision 2) — summarized: `services/agent-orchestration` is a library `apps/api` depends on, so importing `resolvegrid_api.llm_gateway` back into it would be backwards and would saddle a pure orchestration package with FastAPI/SQLAlchemy as transitive dependencies. Dependency injection keeps the graph fully unit-testable with a bare lambda, and reuses `llm_gateway`'s already-tested behavior (Ollama tuning, fallback capture, cost) instead of rebuilding it inside a LangChain wrapper. | `services/agent-orchestration/src/resolvegrid_agent_orchestration/graph.py`, `docs/adr/0002-postgres-checkpointer.md` |
| 2026-08-26 | Checkpoint-restore test proves cross-instance Postgres persistence, not a literal OS process kill | Full reasoning in `docs/adr/0002-postgres-checkpointer.md` (Decision 3) — summarized: a literal process kill is flaky to automate (this project already hit real pain with forceful process termination on Windows during Phase 1); the actual property under test — state surviving a completely independent object graph with only the database in common — is exactly what a real restart depends on too, and is provable directly and deterministically instead. | `apps/api/tests/test_checkpoint_restore.py`, `docs/adr/0002-postgres-checkpointer.md` |
| 2026-08-26 | `classify_intent`'s `risk_level` field has no real enforcement tied to it yet | Matches the approved plan's own "establish the pattern early" framing for stage 2 — this is a deliberate sequencing choice (the field/schema exists before anything gates on it), not an oversight. There is also no `data_scope_tags` field at all yet, since that's inherently retrieval-era (Phase 7+). Revisit trigger: any future stage that needs to route or restrict behavior based on classified risk. | `services/agent-orchestration/src/resolvegrid_agent_orchestration/graph.py` |
| 2026-08-26 | `get_principal`'s debug-header shim still doesn't validate the employee id actually exists, confirmed to cause an unhandled 500 (not a clean 4xx) when a bogus id is supplied to an endpoint that writes a row with a real FK to `employee.id` | Already flagged as accepted "inherited risk" during Phase 6 Task 3's own review for `tickets.py`'s endpoints; independently reproduced during Task 5's manual `/chat` verification via `curl` (a nonexistent employee id → real 500, not 401/403). Not fixed here: the fix belongs in `deps.py`'s `get_principal` itself, a cross-cutting change affecting every router that depends on it, not a single task's scope. A real user is very unlikely to hit this today (the debug header only exists for a real, already-known local employee id), but it's a genuine gap that must close before this codebase's temporary auth shim is ever replaced with anything real. Revisit trigger: the same auth-shim replacement already tracked in the 2026-08-21 `X-Debug-Employee-Id` decision row. | `apps/api/src/resolvegrid_api/deps.py` |
| 2026-08-27 | `services/retrieval`'s embedder calls Ollama's `/api/embed` directly, bypassing LiteLLM, unlike every chat completion in this codebase (`llm_gateway.py`, always via LiteLLM for Anthropic/OpenAI fallback + centralized cost/usage logging) | `infra/litellm/config.yaml` defines no embedding route today (only the three chat `model_name`s), so going through LiteLLM here would mean adding a new route, not reusing one. `nomic-embed-text` is the only embedding model this phase uses and has no cloud-fallback requirement, so LiteLLM's actual value-add (provider abstraction, fallback, budget) doesn't apply yet. `services/retrieval` is also a zero-runtime-dependency library by design (`services/retrieval/pyproject.toml`) that shouldn't gain a dependency on `apps/api`'s LiteLLM master-key/env-var conventions just to reach Ollama. Revisit trigger: a cloud embedding provider (fallback or otherwise) is ever needed, or embedding calls need the same centralized cost/telemetry treatment `ModelCall` gives chat completions today — at that point add a `model_name: nomic-embed-text` entry to `infra/litellm/config.yaml` (`litellm_params.model: ollama/nomic-embed-text`) and route through it like chat calls already do. | `services/retrieval/src/resolvegrid_retrieval/embedder.py` |
| 2026-08-27 | A `Document` with an empty `access_scope_tags` array is treated as public/unscoped — visible to every principal regardless of department, enforced directly in `vector_search`/`lexical_search`'s `WHERE` clause | Needed a default for public-attributable vendor docs (`Document.source_type == "public"`), which have no single owning department to tag them with — an empty array is the natural "no restriction" representation, not a bug. Verified directly (a hand-written test, not just code review): a principal resolving to an *empty* allowed-tags set (fail-closed — no department, no grant) still correctly sees untagged/public documents while a tagged document stays excluded, so this isn't accidentally "empty filter = see everything." The real risk this creates is upstream, not in the filter itself: if Task 6's ingestion pipeline ever fails to tag a `synthetic_private` document that should have been department-restricted, that document silently becomes world-readable rather than erroring — a fail-*open* outcome for exactly the kind of mistake ingestion code is likely to make once real content variety shows up. Revisit trigger / required follow-up: Task 6 must validate at ingestion time that every `synthetic_private` document gets a non-empty `access_scope_tags` (reject or flag ingestion otherwise) so this default only ever applies to genuinely public content, never to a restricted document that fell through a tagging bug. | `apps/api/src/resolvegrid_api/retrieval_authz.py`, `apps/api/src/resolvegrid_api/retrieval.py` |
| 2026-08-27 | `ingest_document`'s title-as-natural-key idempotency has no DB-level unique constraint on `document.title`, and no protection against a race between two concurrent first-ingestion calls for the same brand-new title | Task 6's initial implementation validated the untagged-`synthetic_private` leak check only against the *current call's own arguments* — a review caught that this left a real bypass: re-ingesting a genuinely different, differently-classified document under an *already-existing* title silently kept the OLD document's `source_type`/`access_scope_tags`, discarding the new call's explicit classification (reachable via re-ingestion, not just fresh creation — reproduced directly against the live dev DB, then fixed by re-validating the incoming classification against the existing row and raising `IngestionError` on any mismatch, with a regression test). That fix closes the *sequential* collision case. A genuine *concurrent* race (two simultaneous first-ingestion calls for the same new title, both passing the "not found" lookup before either commits) is not addressed — nothing at the DB layer would prevent two `Document` rows with the identical title, which would silently break the "title = natural key" invariant going forward. Not exploitable today: the seed-corpus runner (`ingestion_worker.py`) ingests sequentially within one Arq job, and only one ingestion job is ever enqueued at a time in this phase's scope. Revisit trigger: any future path that could enqueue concurrent ingestion of the same title (e.g. a re-ingestion API endpoint, parallel corpus workers) — fix via a real unique constraint/index on `document.title` (a new migration) plus handling the resulting `IntegrityError` as a retry-as-update rather than a crash. | `apps/api/src/resolvegrid_api/ingestion.py` |
| 2026-08-28 | Test cleanup in `test_ingestion_worker.py`/`test_chat_api.py` must track rows by id-watermark, not by matching the seed corpus's document titles; and the full test suite must only ever be run against a database with zero Phase-7 knowledge content, with seeding/ingestion strictly as the last, untested step of any verification sequence | Task 10's fresh-state verification deliberately left the seed corpus (8 docs/36 chunks) and 75 seeded employees in the dev DB as Phase 7's final state (see `docs/PROGRESS.md`'s Phase 7 row). Re-running the full `apps/api` suite afterward (an extra sanity check, not part of the established procedure) surfaced two real problems, found empirically: (1) `test_ingestion_worker.py`'s Arq test and `test_chat_api.py`'s `seed_corpus_ingested` fixture cleaned up by matching `Document.title` against the seed corpus's titles — since `ingest_document` is idempotent (a title match on identical content creates zero new rows), this silently deleted the deliberately-persisted corpus the moment either test ran against an already-populated DB, which also cascaded into resetting the employee directory via `test_seed.py`'s pre-existing `manager_id`-chain gap (Phase 6's already-documented seed.py limitation). Fixed by tracking created rows via `id > max-id-before-this-test-ran` in both files, matching the pattern already used for `IngestionRun` in the same tests — verified by wiping to empty, confirming `apps/api/tests` passes clean (107/107), then re-seeding/re-ingesting and confirming nothing gets deleted. (2) A second, structural issue the id-watermark fix does *not* solve: several tests' assertions implicitly assume no real seed-corpus content exists in the DB when they run, and genuinely fail (not flake) once the corpus is actually present — confirmed in two independent files, not an isolated case: `test_retrieval.py`'s fixed-content lexical/vector-search tests assert exact result lists against hand-constructed fixture chunks, and `test_chat_api.py::test_chat_success_writes_agent_run_and_four_success_spans` asserts empty citations for a "What is a VPN?" query on the (documented, correct-at-the-time) assumption that its non-seed-corpus-department requester would find nothing to cite — which stops being true once the corpus's untagged public "What Is a VPN" document is actually ingested and visible to every principal. This is not fixable at the test-cleanup layer — it means the full suite can never be safely re-run once the corpus is ingested. Revisit trigger: if a future phase's own verification procedure needs to run the full suite *after* seeding/ingestion (rather than strictly before), each such test would need isolating (e.g. a dedicated schema/scope, a query guaranteed not to match any real corpus content, or replacing exact-list/empty-list assertions with membership/ordering-relative checks that tolerate extra unrelated content) rather than assuming an empty corpus. | `apps/api/tests/test_ingestion_worker.py`, `apps/api/tests/test_chat_api.py`, `apps/api/tests/test_retrieval.py` |
| 2026-08-28 | Reranker model: `BAAI/bge-reranker-base`, not the master plan's first-named `bge-reranker-v2-m3` | The plan named `v2-m3` with `bge-reranker-base` as a fallback "if the footprint is too heavy." Both were measured for real, CPU-only, in this dev environment (no CUDA toolchain assumed for `services/retrieval`, unlike local Ollama's GPU residency): `v2-m3` (568M params) costs a 2.2GB download, ~134s first load, and ~4.08s warm inference for a realistic 12-candidate batch (~340ms/candidate); `bge-reranker-base` (278M params) costs 1.1GB, ~66s first load, and ~0.94s warm inference for the same batch (~78ms/candidate). `v2-m3`'s ~4s reranking latency doesn't fit a `/chat` request's budget when reranking is one step among retrieval + LLM generation; `v2-m3`'s main real advantage (BAAI's published multilingual benchmarks) buys nothing on this English-only internal IT-ops corpus. The plan's stated fallback turned out to be the real choice, not a hedge. `sentence-transformers` (over BAAI's own `FlagEmbedding`, which additionally pulls training-oriented dependencies this module never uses) was chosen as the library for the same "inference-only, lighter footprint" reasoning. | `services/retrieval/src/resolvegrid_retrieval/reranker.py` |
| 2026-08-28 | `sentence-transformers`/`torch`/`transformers` declared as `resolvegrid-retrieval`'s optional `[reranker]` extra, not a core dependency of that package or of `apps/api` — and a real CI leak this created (a plain `uv sync --all-packages` silently pulling the extra in anyway via the shared `dev` dependency-group) was found and closed, not just designed around on paper | Adding `sentence-transformers` unconditionally would force every environment that installs `resolvegrid-retrieval` — including `apps/api`'s, which uses `chunker.py`/`embedder.py` but didn't call the reranker until Task 7 — to pay a ~200MB/28-package torch install for functionality it doesn't use. The optional-extra mechanism is the standard packaging answer, and this repo's shared `uv` workspace venv means `uv sync --all-packages --extra reranker` (run once, at the root) makes the reranker importable from `apps/api` with zero `apps/api/pyproject.toml` change. During implementation, `sentence-transformers` was briefly duplicated into `resolvegrid-retrieval`'s plain `dev` dependency-group so local test runs could exercise `reranker.py` without the extra — this was wrong and was caught and reverted: `uv sync --all-packages` (every CI job's actual sync command, including jobs that never touch reranking) includes the `dev` group for every workspace member, so the duplication silently reintroduced the exact "every job pays for torch it never uses" cost the extra exists to prevent, through a side door. `.github/workflows/ci.yml`'s `api`/`retrieval` jobs now explicitly opt in via `--extra reranker`; every other job's plain `uv sync --all-packages` stays torch-free, confirmed by inspection of the workflow. | `services/retrieval/pyproject.toml`, `.github/workflows/ci.yml` |
| 2026-08-28 | Context budget assembly skips an oversized chunk entirely rather than truncating it to fit | `verify_citations` (Task 4) checks "was chunk id X present in the context the model was given," not "was *all* of chunk id X's text present." Truncating a chunk to fit the remaining token budget would let a citation to that chunk look fully valid while the model may have only seen a fragment — possibly cut off mid-sentence or mid-qualifier (e.g. silently losing a trailing "...with no opt-out" on an MFA policy). A confidently-cited partial fact is a strictly worse failure mode in an IT-ops/policy context than the model honestly stating the context doesn't fully answer the question, which the prompt template already instructs as the fallback. `assemble_context` continues past a skipped chunk to try subsequent lower-priority candidates, so one oversized chunk only costs itself, not the rest of the budget pass — mitigating the obvious downside (losing the single best-ranked result if it's long). | `services/retrieval/src/resolvegrid_retrieval/context_budget.py` |
| 2026-08-28 | Citation verification's graph-level consequence for a fabricated citation is to strip just that citation's marker from the answer text, not to abstain or discard the whole answer | The plan doc's stage 12 language names "safe answer, abstention, or escalation" as options; this task's brief set a floor of "at minimum, strip fabricated citations." A fabricated `[chunk:<id>]` means one claimed source is untrustworthy, not that the entire answer is — the rest may still be a correct, useful response (e.g. a mostly-general-knowledge answer that hallucinated one spurious citation on an otherwise sound sentence). Exact character-span surgery (`Citation.start`/`end`) removes only the fabricated marker, leaving surrounding prose and genuinely verified citations untouched. A harder consequence (abstain/escalate the whole answer on any fabrication) was considered and rejected as disproportionate for this phase — flagged as a real, deferred gap: a future task with a stricter trust bar (e.g. `risk_level == "high"` and any fabrication at all) could route to hard abstention instead, once that policy is deliberately decided rather than assumed here. | `services/agent-orchestration/src/resolvegrid_agent_orchestration/graph.py` (`verify_citations_node`), `services/retrieval/src/resolvegrid_retrieval/citation_verification.py` |
| 2026-08-28 | Status-aware distractor mitigation uses a full-range score penalty (`DEFAULT_SUPERSEDED_PENALTY = 1.0`, subtracted from every `Document.status == "superseded"` candidate's `[0,1]` rerank score) rather than a smaller or multiplicative discount, and deprioritizes rather than filters superseded candidates | Task 6 measured the regression this fixes as a thin, non-robust cross-encoder margin (0.989 vs. 0.980 — a 0.009-point gap). A penalty tuned only to survive that specific gap would be a fragile, case-specific fix, not a real guarantee — the plan explicitly called for "a real engineering decision, not a rubber-stamp." Since `rerank()`'s output is a sigmoid probability that can never exceed 1.0, subtracting a full 1.0 from every superseded candidate guarantees any non-superseded candidate always sorts first whenever both are present, for any margin the cross-encoder might produce, not just the one measured so far — verified by re-measurement showing the previously-flipped case moved to a wide +8 margin, not a narrow correct order. Deprioritizing rather than filtering was chosen so a superseded chunk can still reach the final context if no non-superseded chunk covers the same point (e.g. a user genuinely asking about the old policy) — matching `dedup.py`'s established "erring toward under-hiding, not silently deleting real content" philosophy. | `services/retrieval/src/resolvegrid_retrieval/status_adjustment.py`, `docs/EXPERIMENT_REGISTRY.md`'s `phase8_reranking_v1`/`phase8_reranking_v2` entries |
| 2026-08-29 | Prompt-injection fix uses explicit `<retrieved_context>`/`</retrieved_context>` delimiter tags plus an untrusted-data framing paragraph naming concrete injection phrasing examples, rather than e.g. a separate structured message role, output-side filtering, or a smaller guard model | A real live-model probe against `qwen3:14b` confirmed Phase 7's undelimited prompt was exploitable (0/2 real runs resisted a "SYSTEM OVERRIDE"-style embedded instruction). `complete_fn`'s contract (`CompleteFn = Callable[[str], str]`, per the Phase 6 dependency-injection decision) is a single flat prompt string with no message-role structure to lean on, so a delimiter-plus-framing approach within that single string was the fix available without changing the completion interface `apps/api`/`services/agent-orchestration` already share. Verified, not assumed: re-run against the same live model on two different injection framings (the exact aggressive payload, and a milder one that already didn't reproduce the failure pre-fix) — 3/3 real runs resisted each, byte-identical output across all runs, a full reversal of the pre-fix aggressive-framing result. Reported honestly as prompt engineering, not a cryptographic guarantee — a sufficiently novel adversarial payload could still succeed in principle; a defense-in-depth output-side check is documented as real future work, not implemented here. | `services/agent-orchestration/src/resolvegrid_agent_orchestration/graph.py` (`_COMPOSE_PROMPT_WITH_CONTEXT_TEMPLATE`), `services/agent-orchestration/tests/test_injected_document_adversarial.py`, `docs/SECURITY.md` |
| 2026-08-29 | Phase 9 Task 3: `operational_adapters/entitlements.py` lives inside `apps/api`, not a new top-level `services/operational-adapters` uv workspace package as the plan-mode architecture doc's §5 layout originally sketched | Building it as a separate package would require that package to import `apps/api`'s `Employee`/`EmployeeEntitlement`/`Entitlement`/`AccessGroup` ORM models directly (Task 1 placed them in `resolvegrid_api.models.org`, and there is no DB-free way to query/insert them) — exactly the "app importing a library that imports back into the app" anti-pattern `services/agent-orchestration/graph.py`'s module docstring already documents and rejects. This is the same tension `apps/api/src/resolvegrid_api/knowledge_store.py`'s docstring resolved once for Phase 7 (`services/retrieval` stays a pure, DB-free library; the SQLAlchemy-touching glue lives in `apps/api`) — Task 3 follows that same twice-precedented resolution rather than inventing a third pattern. Result: a plain module (`operational_adapters/entitlements.py`) that takes a SQLAlchemy `Session` as a parameter, exactly like `knowledge_store.py`/`retrieval.py` — no new workspace package, no new `pyproject.toml`, no new CI job (confirmed: `pyproject.toml`'s `[tool.uv.workspace] members` list and `.github/workflows/ci.yml` both remain unchanged by this phase). | `apps/api/src/resolvegrid_api/operational_adapters/entitlements.py`, `docs/superpowers/plans/2026-09-02-phase9-tools-approvals.md` (Task 3 amendment note) |
| 2026-08-29 | Phase 9 Task 5: `request_approval_for_agent`'s idempotency identity deliberately excludes `expires_at`/`snapshot_hash`; the concurrent-insert race that identity leaves open is closed with a Postgres `pg_advisory_xact_lock`, not a DB uniqueness constraint | A real wall-clock hazard, not hypothetical: LangGraph re-executes an interrupted node's entire body — including the call that computes `expires_at = now() + 24h` — on every resume/restart (verified against installed `langgraph==1.2.11` source). Keying idempotency on `snapshot_hash` equality (the plan's literal wording) would mean every resume recomputes a different `expires_at`, hence a different hash, hence never finds the existing row — inserting a fresh duplicate `ApprovalRequest` on every single resume, defeating the task's own purpose. Fix: identity is checked on the caller-supplied fields only (`agent_run_id`/`action_type`/`action_params_json`/`requested_by_id`/`bound_evidence_refs_json`/`risk_context`); `expires_at`/`snapshot_hash` are computed exactly once, at the moment a brand-new row is inserted, and never touched again. A composite `UniqueConstraint` over that identity tuple was considered and rejected for the resulting concurrent-insert race: several of those columns are nullable, and Postgres unique constraints treat NULL as distinct-from-every-other-NULL by default, so a plain constraint would silently fail to dedupe the common all-`None` case; a NULLS-NOT-DISTINCT/COALESCE expression index was judged more complex than needed, and `action_params_json` is unbounded text that risks exceeding a btree index's size limit. Instead, `request_approval_for_agent` wraps its whole check-then-insert section in `pg_advisory_xact_lock(key)`, keyed on a sha256-derived hash of that same identity tuple — transaction-scoped, so it releases automatically at commit, with no separate unlock call. Proven under genuine concurrency (not just sequential re-execution) by `apps/api/tests/test_approval_service.py`'s `test_request_approval_for_agent_closes_the_concurrent_insert_race`. | `apps/api/src/resolvegrid_api/approval_service.py` |
| 2026-08-29 | Phase 9 Task 6: `execute_mutation` requires the caller's `tool_name`/`tool_params` to exactly equal the `ApprovalRequest` row's own (hash-verified) `action_type`/parsed `action_params_json`, raising `ApprovalParamsMismatchError` (a subclass of `ApprovalTamperError`) on mismatch; dispatch always uses the row's own `stored_params`, never the caller-supplied ones; the duplicate-replay guard's own check-then-act race is closed with a second `pg_advisory_xact_lock`, keyed on the plain integer `approval_request_id` | A real confused-deputy gap in the plan's literal step ordering, not a hypothetical: the snapshot-hash tamper check only re-verifies the ROW's stored fields against themselves — it says nothing about whether the `tool_params` argument passed into *this specific call* matches what was actually approved. Taken literally, a caller could present a valid, unexpired, approved `approval_request_id` alongside a completely different `tool_params` (e.g. a different `employee_id`) and the function would happily grant access to whoever the caller named, not whoever was actually approved — silently defeating the entire approval gate while every tamper/status/expiry check still reported green. Treated as the same category of defect as a snapshot-hash mismatch (an attempt to execute something other than what was approved), not a softer error, but given its own subclass/name so the audit trail (`ToolCall.error_taxonomy_code`) can still forensically distinguish "the stored row was altered" from "the caller's params didn't match an otherwise-untampered row." Separately: `ToolCall.idempotency_key` is indexed but not unique-constrained (same NULL-safety reasoning as `ApprovalRequest`'s identity tuple above), so the duplicate-replay SELECT-then-insert is its own check-then-act race — closed the identical way, a `pg_advisory_xact_lock(approval_request_id)` acquired immediately after fetching the row and before any check runs, so two concurrent callers racing the same approval are fully serialized for the function's entire body. Unlike Task 5's hashed composite lock key, this one needs no hashing — `approval_request_id` is already a single primitive int within Postgres's signed-64-bit advisory-lock key range. Both real gaps were caught and fixed during this task, not found later — see `apps/api/tests/test_mutation_execution.py`'s `test_execute_mutation_raises_params_mismatch_error_when_tool_params_argument_does_not_match_approved_params`, `test_execute_mutation_tamper_and_params_mismatch_produce_distinguishable_error_taxonomy_codes`, and `test_execute_mutation_closes_the_concurrent_replay_race` (genuine concurrency, real DB row-count assertion, not a mock). | `apps/api/src/resolvegrid_api/mutation_execution.py` |
| 2026-09-04 | Phase 9 Task 7a: a second, separate compiled LangGraph graph (`build_tool_invocation_graph`), sharing the SAME `AsyncPostgresSaver` checkpointer instance as the main chat graph, rather than teaching the chat graph to infer tool intent from free text | Tasks 4/5/6 each correctly, deliberately declined to wire `request_approval`/`execute_mutation` into `build_graph`'s conditional routing (each task's own docstring says so) — leaving a real gap: as of Task 6, nothing in the running app ever actually called `interrupt()` for real outside of throwaway single-node test harnesses, and Task 8's restart-mid-approval exit-criteria test needs a REAL paused run to restart against. Teaching the existing chat pipeline to infer tool intent from arbitrary user text would be real LLM-driven tool selection — a materially larger, separate undertaking no task in this phase specifies, and risks regressing the well-tested chat flow for a use case (an analyst deliberately choosing a named action from a form) that doesn't need free-text inference at all. Two infra facts were independently verified before committing to the shared-checkpointer design, not assumed: (a) `AsyncPostgresSaver` checkpoint rows are keyed only by `(thread_id, checkpoint_ns)` from the caller's own config, never by which compiled `Pregel`/`StateGraph` object made the call (confirmed by reading the installed `langgraph-checkpoint-postgres` source, `aget_tuple`/`aput`) — so two different compiled graphs sharing one checkpointer is exactly as safe as two different `thread_id`s ever calling the same graph, provided a given `thread_id` is never invoked against both graphs (guaranteed here by `routers/tools.py`'s fresh-`uuid4()`-per-invocation convention); (b) an interrupted `ainvoke()`'s real, durable `approval_request_id` must be read from `result["__interrupt__"][0].value`, not from the returned state dict, since the interrupted node's own `return` statement never executes on that call — confirmed against the installed `langgraph==1.2.11` runtime, not assumed from prose docs (whose HIL pages 404/redirect as of this writing). | `services/agent-orchestration/src/resolvegrid_agent_orchestration/graph.py` (`build_tool_invocation_graph`), `apps/api/src/resolvegrid_api/routers/tools.py` |
| 2026-08-29 | Phase 9 Task 4: `validate_tool_schema` uses the `jsonschema` library (a new `apps/api` dependency), not a hand-rolled required-keys/type check | `ToolContract.params_schema` is authored as real JSON Schema (`additionalProperties`/`required`/per-property `type` keys — see `TOOL_REGISTRY`), and `jsonschema` is a small, pure-Python, widely-used library with no heavy transitive deps (unlike e.g. the reranker's torch pull) — re-implementing a subset of JSON Schema semantics inline would just be a second, drifting copy of what `jsonschema` already does correctly. `validate_tool_schema` does not guard against `jsonschema.exceptions.SchemaError` (a malformed `params_schema` itself): `params_schema` is static, developer-authored registry content, not user/model-controlled input, so a malformed schema is a registry bug to fix at the source, not a runtime condition to translate into `ToolValidationError`. | `apps/api/src/resolvegrid_api/tool_execution.py`, `apps/api/pyproject.toml` |
| 2026-09-04 | Phase 9 Task 8: the restart-mid-approval test's first draft skipped writing the `ApprovalDecision`/status-flip and called `Command(resume="approved")` directly against a still-`"pending"` row | `execute_mutation_for_agent` correctly refused with `ApprovalNotDecidedError` (`status='pending'` is not `"approved"`) — a real finding, not a test bug to silently paper over: it proves `execute_mutation`'s status check is a real, live gate, not a formality, and that a correct restart-mid-approval test must reproduce a real approver-decide caller's FULL write sequence (`ApprovalDecision` insert + `ApprovalRequest.status` flip, committed, THEN the resume call — mirroring `routers/approvals.py`'s `decide_approval` exactly) rather than calling `Command(resume=...)` in isolation. The test was corrected to perform that same write sequence between instance A (reaching `interrupt()`) and instance B (resuming), and only then reached the success path it was actually meant to verify. | `apps/api/tests/test_restart_mid_approval.py` |
| 2026-09-04 | Phase 9 Task 7a: the real `ExecuteMutationFn` implementation (`execute_mutation_for_agent`) lives in its own new file, `apps/api/src/resolvegrid_api/agent_mutation_execution.py`, not folded into `approval_service.py` | `mutation_execution.py` already does `from resolvegrid_api.approval_service import compute_snapshot_hash` at module scope; adding `execute_mutation_for_agent` (which must call `mutation_execution.execute_mutation`) into `approval_service.py` instead would create a circular import between the two modules. A new, small module (mirroring `agent_retrieval.py`'s naming/placement convention — the real `RetrieveFn` implementation for the same graph package) avoids that without restructuring either existing file. | `apps/api/src/resolvegrid_api/agent_mutation_execution.py` |
| 2026-09-04 | Phase 9 Task 7b: `GET /approvals` applies no department scoping — any principal passing the staff-only `approval.list` authz check sees every pending `ApprovalRequest` company-wide | `ApprovalRequest` has no department column, and this phase's only registered `action_type` (`grant_vpn_access`) has no per-department routing semantics anywhere yet — `ApprovalPolicy.stages_json` (Task 1's schema) exists but nothing populates or reads it. Building a real join would mean inventing a department-attribution rule (e.g. "the requester's own department") that no requirement in this phase actually specifies, and would look more precise than it actually is. `approval.list`/`approval.decide` were added to `packages/authz/policy.py`'s `_STAFF_ONLY_ACTIONS` (never `_SELF_SCOPED_ACTIONS`, matching `ticket.transition`'s precedent) so a principal with no admin/department-scoped analyst-or-approver grant is denied outright rather than silently downgraded to a self-scoped view that would incorrectly let them see/decide approvals they merely requested or are the subject of. Revisit trigger: a future phase adding a second `action_type` with genuine department semantics, or wiring up `ApprovalPolicy` staging for real — at that point this list query needs a real department join, not a silent assumption that it already filters. | `apps/api/src/resolvegrid_api/routers/approvals.py`, `packages/authz/src/resolvegrid_authz/policy.py` |
