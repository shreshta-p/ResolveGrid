[project]
name = "resolvegrid-workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["apps/api", "packages/telemetry", "packages/authz"]

[tool.uv]
package = false

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
